{
  "name": "tt-agent-bridge",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && vite build",
    "watch": "vite build --watch"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.0.0"
  }
}
